# Command Line Applications

This directory contains main applications and CLI tools.

## Applications
- Service binaries will be built here
- Each service gets its own subdirectory
- CLI tools for management and testing

## Build
```bash
make build          # Build all services
make build-gateway  # Build specific service
```
